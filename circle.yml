machine:
  environment:
    HOST: "http://localhost"
    SEARCHBOX_SSL_URL: "http://localhost:9200"
    ADMIN_BASIC_AUTH: "name:password"
    DEFAULT_USERS: "test@example.com"
    BUCKETEER_BUCKET_NAME: 'example-bucket'
test:
  post:
    - bundle exec rubocop
    - bundle exec haml-lint .
