{
  "name": "openownership-register",
  "scripts": {
  },
  "env": {
    "BASIC_AUTH": {
      "required": true
    },
    "HEROKU_APP_NAME": {
      "required": true
    },
    "OPENCORPORATES_API_TOKEN": {
      "required": true
    },
    "ROLLBAR_ACCESS_TOKEN": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [
    "mongolab:sandbox"
  ],
  "buildpacks": [
  ]
}
