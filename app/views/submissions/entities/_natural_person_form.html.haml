= form_with form_options_for_entity(entity).merge(model: entity, scope: :entity, local: true) do |f|
  = render 'submissions/shared/form_errors', errors: entity.errors if entity.errors.any?
  = render 'continue_params'
  = f.hidden_field :type
  = f.hidden_field :user_created
  .form-group
    = f.label :name
    = f.text_field :name, id: "entity_name", class: 'form-control form-control-lg', autofocus: true
    %small= t('.name_hint')
  %hr
  .form-group
    = f.label :dob, t('.dob_label')
    = f.date_field :dob, placeholder: 'YYYY-MM-DD', id: "entity_dob", class: 'form-control', value: partial_date_format(entity.dob)
    %small= t('.dob_hint')
  .form-group
    = f.label :country_of_residence
    = f.select :country_of_residence, countries_for_select, { prompt: '' }, id: "entity_country_of_residence", class: 'form-control'
  .form-group
    = f.label :nationality
    = f.select :nationality, countries_for_select, { prompt: '' }, id: "entity_nationality", class: 'form-control'
  .form-group
    = f.label :address, t('.address_label')
    = f.text_field :address, id: "entity_address", class: 'form-control'
    %small= t('.address_hint')
  .form-group
    = f.button t('.continue'), class: 'btn btn-primary'
